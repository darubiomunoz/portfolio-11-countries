import styled from 'styled-components';

import { spacingRegular, spacingNormal } from '../../Variables';

const StyledButtonSection = styled.section`
  width: 100%;
  height: fit-content;
  padding: ${spacingNormal} ${spacingRegular};
`;

export default StyledButtonSection;